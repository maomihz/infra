[Unit]
Description=KMS Emulator in C
After=network.target

[Service]
ExecStart={{ vlmcsd.prefix }}/bin/vlmcsd -e -L {{ vlmcsd.listen }}
KillSignal=SIGINT
Type=forking
User=nobody
Group=nogroup


[Install]
WantedBy=default.target